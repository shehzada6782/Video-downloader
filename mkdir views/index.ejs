<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #0866ff 0%, #E1306C 100%);
            color: white;
            padding: 30px;
            text-align: center;
            position: relative;
        }

        .platform-badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(255,255,255,0.2);
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            backdrop-filter: blur(10px);
        }

        .header h1 {
            font-size: 2.3em;
            margin-bottom: 10px;
        }

        .header p {
            opacity: 0.9;
            font-size: 1.1em;
        }

        .content {
            padding: 40px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .url-input {
            width: 100%;
            padding: 15px 20px;
            border: 2px solid #e1e8ed;
            border-radius: 50px;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .url-input:focus {
            outline: none;
            border-color: #0866ff;
            box-shadow: 0 0 0 3px rgba(8, 102, 255, 0.1);
        }

        .download-btn {
            background: linear-gradient(135deg, #0866ff 0%, #E1306C 100%);
            color: white;
            border: none;
            padding: 15px 40px;
            border-radius: 50px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }

        .download-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(8, 102, 255, 0.3);
        }

        .download-btn:active {
            transform: translateY(0);
        }

        .error {
            background: #ffe6e6;
            color: #d63031;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
            border-left: 4px solid #d63031;
        }

        .success {
            background: #d4edda;
            color: #155724;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
            border-left: 4px solid #28a745;
        }

        .video-info {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .video-thumbnail {
            max-width: 100%;
            border-radius: 10px;
            margin-bottom: 15px;
            max-height: 300px;
            object-fit: cover;
        }

        .download-links {
            display: grid;
            gap: 15px;
        }

        .download-link {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #f8f9fa;
            padding: 15px 20px;
            border-radius: 10px;
            text-decoration: none;
            color: #333;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .download-link:hover {
            background: #0866ff;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        .quality-badge {
            background: #0866ff;
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .platform-tabs {
            display: flex;
            margin-bottom: 20px;
            background: #f8f9fa;
            border-radius: 50px;
            padding: 5px;
        }

        .platform-tab {
            flex: 1;
            padding: 10px;
            text-align: center;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 600;
        }

        .platform-tab.active {
            background: linear-gradient(135deg, #0866ff 0%, #E1306C 100%);
            color: white;
        }

        .instructions {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            color: #856404;
            padding: 20px;
            border-radius: 10px;
            margin-top: 30px;
        }

        .instructions h3 {
            margin-bottom: 10px;
        }

        .instructions ol {
            margin-left: 20px;
        }

        .instructions li {
            margin-bottom: 8px;
        }

        .url-examples {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-top: 15px;
        }

        .example {
            background: rgba(255,255,255,0.5);
            padding: 10px;
            border-radius: 8px;
            font-size: 12px;
            border-left: 3px solid #0866ff;
        }

        .example.instagram {
            border-left-color: #E1306C;
        }

        .footer {
            text-align: center;
            padding: 20px;
            background: #f8f9fa;
            color: #666;
            font-size: 14px;
        }

        @media (max-width: 600px) {
            .url-examples {
                grid-template-columns: 1fr;
            }
            
            .header h1 {
                font-size: 1.8em;
            }
            
            .content {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <% if (platform) { %>
                <div class="platform-badge">
                    <%= platform.charAt(0).toUpperCase() + platform.slice(1) %>
                </div>
            <% } %>
            <h1>📹 Social Video Downloader</h1>
            <p>Download Facebook & Instagram videos easily</p>
        </div>

        <div class="content">
            <% if (error) { %>
                <div class="error">
                    <strong>Error:</strong> <%= error %>
                </div>
            <% } %>

            <% if (videoInfo) { %>
                <div class="success">
                    <strong>Success!</strong> Video found and ready to download
                </div>
            <% } %>

            <form action="/download" method="POST">
                <div class="form-group">
                    <input 
                        type="url" 
                        name="videoUrl" 
                        class="url-input" 
                        placeholder="Paste Facebook or Instagram video URL here..." 
                        required
                        value="<%= videoInfo ? videoUrl : '' %>"
                    >
                </div>
                <button type="submit" class="download-btn">
                    🚀 Download Video
                </button>
            </form>

            <div class="url-examples">
                <div class="example">
                    <strong>Facebook Examples:</strong><br>
                    • https://facebook.com/...<br>
                    • https://fb.watch/...<br>
                    • https://www.facebook.com/...
                </div>
                <div class="example instagram">
                    <strong>Instagram Examples:</strong><br>
                    • https://instagram.com/p/...<br>
                    • https://www.instagram.com/reel/...<br>
                    • https://instagr.am/p/...
                </div>
            </div>

            <% if (videoInfo) { %>
                <div class="video-info">
                    <h3>🎯 Video Found!</h3>
                    <% if (videoInfo.thumbnail) { %>
                        <img src="<%= videoInfo.thumbnail %>" alt="Video Thumbnail" class="video-thumbnail" onerror="this.style.display='none'">
                    <% } %>
                    <p><strong>Platform:</strong> <%= videoInfo.platform ? videoInfo.platform.charAt(0).toUpperCase() + videoInfo.platform.slice(1) : 'Social Media' %></p>
                    <p><strong>Title:</strong> <%= videoInfo.title %></p>
                </div>
            <% } %>

            <% if (downloadLinks && downloadLinks.length > 0) { %>
                <div class="download-links">
                    <h3>📥 Download Links:</h3>
                    <% downloadLinks.forEach(link => { %>
                        <a href="<%= link.url %>" class="download-link" target="_blank" download>
                            <span>⬇️ Download Video</span>
                            <span class="quality-badge"><%= link.quality %></span>
                        </a>
                    <% }); %>
                </div>
            <% } %>

            <div class="instructions">
                <h3>📋 How to Use:</h3>
                <ol>
                    <li>Go to <strong>Facebook</strong> or <strong>Instagram</strong> and find the video</li>
                    <li>Copy the video URL from address bar or share button</li>
                    <li>Paste the URL in the input field above</li>
                    <li>Click "Download Video" button</li>
                    <li>Choose your preferred quality and download</li>
                </ol>
                
                <div style="margin-top: 15px; padding: 10px; background: rgba(255,255,255,0.3); border-radius: 5px;">
                    <strong>💡 Tip:</strong> For Instagram, make sure the account is public and you're copying the URL of a post or reel.
                </div>
            </div>
        </div>

        <div class="footer">
            <p>© 2024 Social Video Downloader | For educational purposes only</p>
        </div>
    </div>

    <script>
        // Simple platform tab functionality (if needed in future)
        document.addEventListener('DOMContentLoaded', function() {
            // Add any interactive features here if needed
        });
    </script>
</body>
</html>
